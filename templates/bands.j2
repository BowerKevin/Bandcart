<html>
    <head>
        <link rel="stylesheet" type="text/css" href="./static/css/style.css">
    </head>
    <h2>Add, Update, or Delete a Band</h2>
    <p>To add a Band, all fields can be NULL except for the <strong>Band name</strong>.</p>

    <form method="POST">
        <fieldset>
            <legend>Add a Band: </legend>
            <label for="bandName">Band name: </label>
            <input type="text" id="bandName" name="bandName">
            <label for="numMembers">Number of Members: </label>
            <input type="number" id="numMembers" name="numMembers">
            <label for="genre">Genre: </label>
            <input type="text" id="genre" name="genre">
            <input type="submit" id="addBand" value="Add">
        </fieldset>
    </form>
    

    {% if Bands != () %}
        <table>
        <thead>
        <tr>
            <!-- Iterate through each key in the first entry to get the
            column name -->
            {% for key in Bands[0].keys() %}

            <!-- Create a <th> tag with the key inside of it, this will be
            our header row -->
            <th>{{ key }}</th>

            <!-- End of this for loop -->
            {% endfor %}
            <th>update</th>
            <th>delete</th>
        </tr>
        <tbody>

        <!-- Now, iterate through every person in bsg_people -->
        {% for Band in Bands %}
        <tr>
            <!-- Then iterate through every key in the current person dictionary -->
            {% for key in Band.keys() %}

            <!-- Create a <td> element with the value of that key in it -->
            <td>{{Band[key]}}</td>
            {% endfor %}
            <td>
            <!-- <form method="PUT"><input type="submit" id="updateBand" value="update"><form> -->
            <button type="button" method="PUT">update</button>
            </td>
            <td>
            <button type="button" method="DELETE">delete</button>
            </td>
        {% endfor %}
        </table>
    {% endif %}
</html>